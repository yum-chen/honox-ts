---
title: Switch Commutateur
hydration: 2
category: Forms
---

import { Switch } from "../../../app/components/ui";
import { DemoPreview } from "../../../app/components/demo-preview";

# Introduction

A control that allows the user to toggle between checked and unchecked states.

# Utilisation

<DemoPreview>
  <Switch defaultChecked interactive>Enable Notifications</Switch>
</DemoPreview>

```tsx
import { Switch } from "../components/ui";

export default function MyPage() {
  return (
    <Switch
      defaultChecked={true}
      onCheckedChange={(details) => console.log(details.checked)}
      interactive
    >
      Enable Notifications
    </Switch>
  );
}
```

## Générateur de page CMS

Ce composant est disponible en tant que bloc `switch` dans le [Générateur de page](/docs/PageBuilder) (`content/pages/*.json`) :

```json
{
  "type": "switch",
  "label": "Dark Mode",
  "checked": true,
  "colorPalette": "blue"
}
```

# Propriétés

| Propriété | Type | Description |
| --- | --- | --- |
| `children` | `any` | Label content to be rendered next to the switch. |
| `class` | `string` | Classes CSS personnalisées. |
| `checked` | `boolean` | Whether the switch is checked (controlled). |
| `defaultChecked` | `boolean` | The initial checked state (uncontrolled). |
| `disabled` | `boolean` | Whether the switch is disabled. |
| `onCheckedChange` | `(details: { checked: boolean }) => void` | Callback triggered when the checked state changes. |

| `size` | \`"sm" \ | "md" \ | "lg"\` | The size of the switch. |

| `interactive` | `boolean` | Forces hydration as an island. |
