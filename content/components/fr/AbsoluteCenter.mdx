---
title: AbsoluteCenter Centrage Absolu
hydration: 3
category: Layout
---

import { AbsoluteCenter } from "../../../app/components/ui";
import { DemoPreview } from "../../../app/components/demo-preview";

# Introduction

Un composant utilitaire pour centrer le contenu dans son conteneur parent.

# Utilisation

<DemoPreview>
  <div style={{ position: "relative", height: "140px", width: "260px", border: "1px solid var(--colors-border)" }}>
    <AbsoluteCenter axis="both">Centered Content</AbsoluteCenter>
  </div>
</DemoPreview>

```tsx
import { AbsoluteCenter } from "../components/ui";

export default function MyPage() {
  return (
    <div
      style={{
        position: "relative",
        height: "200px",
        width: "200px",
        border: "1px solid black",
      }}
    >
      <AbsoluteCenter axis="both">Centered Content</AbsoluteCenter>
    </div>
  );
}
```

## Constructeur de pages CMS

Ce composant est disponible sous forme de bloc `absoluteCenter` dans [Page Builder](/docs/PageBuilder) (`content/pages/*.json`) :

```markdown
{
  "type": "absoluteCenter",
  "axis": "both",
  "children": [
    { "type": "badge", "text": "Centered Badge", "colorPalette": "purple", "variant": "solid" }
  ]
}
```

# Propriétés

| Propriété | Type | Description |
| --- | --- | --- |
| `children` | `any` | Contenu à restituer à l'intérieur du composant. |
| `class` | `string` | Classes CSS personnalisées. |

| `axis` | \`"horizontal" \ | "verticale" \ | "both"\` | The axis to center the content on. |
