---
title: TagsInput
hydration: 2
category: Forms
---

import { TagsInput } from "../../app/components/ui";
import { DemoPreview } from "../../app/components/demo-preview";

A component that allows users to enter multiple tags as a list.

## Usage

<DemoPreview>
  <div style={{ width: "100%", maxWidth: "320px" }}>
    <TagsInput label="Skills" defaultValue={["React", "Hono"]} />
  </div>
</DemoPreview>

### CMS Page Builder

This component is available as a `tagsInput` block in the [Page Builder](/docs/PageBuilder) (`content/pages/*.json`):

```json
{
  "type": "tagsInput",
  "label": "Amenities",
  "defaultValue": ["WiFi", "Fireplace", "Hot Tub"]
}
```

## Props

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `string[]` | - | Current tags. |
| `defaultValue` | `string[]` | - | Initial tags. |

## CMS Configuration

```yaml
- label: TagsInput
  name: tagsInput
  widget: object
  fields:
    - { label: Label, name: label, widget: string, required: false }
    - { label: Default Tags, name: defaultValue, widget: list, required: false }
```

## Hydration

Tier-2: Hydrates when `value`, `defaultValue`, `onValueChange`, or input state is present.
