import {
	InteractiveSearch,
	type SearchBaseProps,
} from "../components/ui/search-primitive";

export default function SearchIsland(props: SearchBaseProps) {
	return <InteractiveSearch {...props} />;
}
