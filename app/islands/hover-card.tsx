import {
	type HoverCardRootProps,
	InteractiveHoverCardRoot,
} from "../components/ui/hover-card-primitive";

export default function HoverCardIsland(props: HoverCardRootProps) {
	return <InteractiveHoverCardRoot {...props} />;
}
